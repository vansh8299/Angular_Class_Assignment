import { CustomdashboardPipe } from './customdashboard.pipe';

describe('CustomdashboardPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomdashboardPipe();
    expect(pipe).toBeTruthy();
  });
});
